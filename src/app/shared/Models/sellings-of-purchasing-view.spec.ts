import { SellingsOfPurchasingView } from './sellings-of-purchasing-view';

describe('SellingsOfPurchasingView', () => {
  it('should create an instance', () => {
    expect(new SellingsOfPurchasingView()).toBeTruthy();
  });
});
